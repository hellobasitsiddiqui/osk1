<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <!--
      Launch (splash) theme. Parented on androidx.core:core-splashscreen's
      Theme.SplashScreen so the branded splash renders consistently from Android 6
      (minSdk 24) up through Android 12+'s system splash-screen window.

      OSK-158:
        - windowSplashScreenBackground pins the Android 12+ system splash window to
          the OpenSkeleton dark canvas (@color/splash_background = #0d1117) instead
          of the platform default (which can flash white before the app paints).
        - android:background keeps the full-screen branded splash image
          (@drawable/splash — dark canvas + centred "OS" monogram) as the activity
          window background that bridges the system splash to the first WebView
          frame. The @capacitor/splash-screen plugin also shows this same image at
          runtime and dismisses it on-ready (see web/splash.js).
    -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>